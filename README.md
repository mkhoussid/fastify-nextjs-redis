# fastify-nextjs with Redis

## To get started

Make sure you have [`redis-server`](https://redis.io/topics/quickstart) installed and running.

Then run,

```bash
npm install && npm run dev
# or npm ci
```
